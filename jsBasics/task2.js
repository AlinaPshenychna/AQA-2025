const firstName= "Alina";
const secondName = "Olena";

const mainStr = "Hello " + firstName+ " and "+secondName;
console.log(mainStr);

const template = `Hello ${firstName} and ${secondName}`;
console.log(template);